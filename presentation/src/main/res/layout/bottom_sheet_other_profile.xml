<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="vm"
            type="com.plub.presentation.ui.main.profile.bottomsheet.BottomSheetProfileViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_bottom_sheet">

        <TextView
            android:id="@+id/text_view_report"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginStart="28dp"
            android:layout_marginTop="23dp"
            android:text="@string/word_report"
            android:textSize="10sp"
            android:textColor="@color/color_8c8c8c"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <ImageView
            android:id="@+id/image_view_cancel"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="17dp"
            android:layout_marginEnd="6dp"
            android:src="@drawable/ic_x_8c8c8c"
            app:onThrottleClick="@{()->vm.onClickClose()}"
            android:scaleType="center"
            android:layout_width="32dp"
            android:layout_height="32dp"/>

        <androidx.recyclerview.widget.RecyclerView
            android:paddingBottom="64dp"
            android:id="@+id/recycler_view_profile_list"
            app:layout_constraintTop_toBottomOf="@id/image_view_cancel"
            android:layout_marginTop="10dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>
        
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>